'use client';

import { Logo } from '@/components/Logo';

const HomePage = () => {
  return (
    <header>
      <Logo variant="header" />
    </header>
  );
};

export default HomePage;
